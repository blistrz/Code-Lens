# Copy to .env and adjust. Restart runserver after changes.

# Use sqlite when SQL Server is off or unreachable (local dev)
# DB_ENGINE=sqlite

# SQL Server (default when DB_ENGINE is not sqlite)
DB_NAME=CodeLensDB
DB_USER=sa
DB_PASSWORD=YourPassword123
DB_HOST=127.0.0.1
DB_PORT=1433
